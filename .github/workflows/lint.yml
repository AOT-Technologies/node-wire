##
## SPDX-FileCopyrightText: 2026 AOT Technologies
## SPDX-License-Identifier: Apache-2.0
##


name: Lint and Type Check

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lockfile-check:
    name: Lockfile freshness
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Verify uv.lock matches pyproject.toml
        id: lock
        run: uv lock --check

      - name: Report lockfile check
        if: always()
        run: |
          echo "### Lockfile: ${{ steps.lock.outcome }}" >> "$GITHUB_STEP_SUMMARY"

  ruff:
    name: Ruff Linters
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Install dependencies
        run: uv sync --frozen --extra agents --dev

      - name: Run Ruff (Linting)
        id: ruff
        # --output-format=github automatically creates inline annotations on your PR code!
        run: uv run ruff check --output-format=github .

      - name: Run Ruff (Formatting Check)
        id: ruff-format
        run: uv run ruff format --check .

      - name: Report Ruff
        if: always()
        run: |
          {
            echo "### Ruff"
            echo "- Lint: ${{ steps.ruff.outcome }}"
            echo "- Format: ${{ steps.ruff-format.outcome }}"
          } >> "$GITHUB_STEP_SUMMARY"

  reuse:
    name: REUSE compliance
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Install dependencies
        run: uv sync --frozen --extra agents --dev

      - name: Run REUSE lint
        id: reuse
        run: uv run reuse lint

      - name: Report REUSE
        if: always()
        run: |
          echo "### REUSE lint: ${{ steps.reuse.outcome }}" >> "$GITHUB_STEP_SUMMARY"

  mypy:
    name: Mypy Type Check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Install dependencies
        run: uv sync --frozen --extra agents --dev

      - name: Run Mypy (Type checking)
        shell: bash
        run: |
          echo "::add-matcher::.github/matchers/mypy.json"
          set +e
          uv run mypy --show-column-numbers --show-error-codes 2>&1 | tee mypy.txt
          status=${PIPESTATUS[0]}
          set -e
          uv run python scripts/ci_step_summary.py mypy mypy.txt
          summary_status=$?
          if [[ "${status}" -ne 0 ]]; then
            exit "${status}"
          fi
          exit "${summary_status}"
